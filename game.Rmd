---
title: "Penalty Shootout ⚽"
output: 
  html_document:
    toc: false
    toc_float: false
---


```{r, echo=FALSE, results='asis'}
cat('<link rel="stylesheet" href="style.css">')
```


<!-- **游戏容器** -->
<div id="gameContainer">
    
<img id="goal" src="images/goal.png">

    
<img id="player" src="images/player.png">

   
<img id="ball" src="images/ball.png">

   
<p id="gameMessage">🏆 试着射门吧！</p>
</div>

<script>
document.addEventListener("DOMContentLoaded", function() {
    const gameContainer = document.getElementById("gameContainer");
    const player = document.getElementById("player");
    const ball = document.getElementById("ball");
    const goal = document.getElementById("goal");
    const gameMessage = document.getElementById("gameMessage");

    function updateSizes() {
        const containerWidth = gameContainer.offsetWidth;
        const containerHeight = gameContainer.offsetHeight;

        player.style.width = `${containerWidth * 0.12}px`;
        ball.style.width = `${containerWidth * 0.05}px`;
        goal.style.width = `${containerWidth * 0.35}px`;

        resetPositions(containerWidth, containerHeight);
    }

    function resetPositions(containerWidth, containerHeight) {
        playerX = containerWidth * 0.4;
        playerY = containerHeight * 0.8;
        ballX = playerX + 0.2 * playerX;
        ballY = playerY + 0.1 * playerY;
        ballMoving = false;
        ballStopping = false;
        reachedGoalHeight = false;

        goal.style.left = "50%";
        goal.style.top = "10%";
        goal.style.transform = "translateX(-50%)";

        player.style.left = `${playerX}px`;
        player.style.top = `${playerY}px`;
        ball.style.left = `${ballX}px`;
        ball.style.top = `${ballY}px`;

        gameMessage.innerText = "🏆 试着射门吧！";
    }

    let playerX, playerY, ballX, ballY;
    let ballMoving = false;
    let ballStopping = false;
    let reachedGoalHeight = false; // 记录是否达到射门高度

    updateSizes();

    document.addEventListener("keydown", function(event) {
        const containerWidth = gameContainer.offsetWidth;
        const containerHeight = gameContainer.offsetHeight;

        if (event.key === "ArrowLeft" && playerX > 0) {
            playerX -= containerWidth * 0.03;
            if (!ballMoving) ballX -= containerWidth * 0.03;
        }
        if (event.key === "ArrowRight" && playerX < containerWidth - player.offsetWidth) {
            playerX += containerWidth * 0.03;
            if (!ballMoving) ballX += containerWidth * 0.03;
        }

        // 按第一次空格：球开始上升
        if (event.key === " " && !ballMoving) {
            ballMoving = true;
            ballStopping = false; // 允许停止
            let interval = setInterval(() => {
                if (!ballStopping) {
                    ballY -= 10; // 球上升速度
                    ball.style.top = `${ballY}px`;

                    // 记录是否达到球门高度
                    const goalRect = goal.getBoundingClientRect();
                    if (ball.getBoundingClientRect().top <= goalRect.top + goalRect.height * 0.8) {
                        reachedGoalHeight = true;
                    }
                }
            }, 50);

            // 监听第二次空格键，停止球
            document.addEventListener("keydown", function stopBall(event) {
                if (event.key === " " && ballMoving && !ballStopping) {
                    ballStopping = true;
                    clearInterval(interval); // 停止球的上升
                    checkGoal(); // 计算是否进球
                    document.removeEventListener("keydown", stopBall); // 只允许按一次
                }
            });
        }

        player.style.left = `${playerX}px`;
        player.style.top = `${playerY}px`;
        ball.style.left = `${ballX}px`;
        ball.style.top = `${ballY}px`;
    });

    // **进球判定**
    function checkGoal() {
        const goalRect = goal.getBoundingClientRect();
        const ballRect = ball.getBoundingClientRect();

        const ballCenterX = ballRect.left + ballRect.width / 2;
        const ballCenterY = ballRect.top + ballRect.height / 2;
        const goalLeft = goalRect.left;
        const goalRight = goalRect.right;
        const goalTop = goalRect.top + goalRect.height / 2;
        const goalMaxHeight = goalRect.top; // 球门最顶端

        if (!reachedGoalHeight) {
            gameMessage.innerText = "⚠️ 球还没射出去呢！";
        } else if (ballCenterY < goalMaxHeight) {
            gameMessage.innerText = "❌ 太高了，飞出去了！";
        } else  { // 球到达球门高度
            if (ballCenterX >= goalLeft && ballCenterX <= goalRight) {
                gameMessage.innerText = "🥳 进球啦！";
            } else {
                gameMessage.innerText = "❌ 射偏了！";
            }
        }

        setTimeout(() => {
            updateSizes();
        }, 1000);
    }

    window.addEventListener("resize", updateSizes);
});
</script>



<style>
/* 让整个网页作为游戏背景 */
body {
    background: linear-gradient(to right, #ff9a9e, #fad0c4);
    text-align: center;
    font-family: "Poppins", sans-serif;
    color: #4A4A4A;
}

/* 游戏容器，使得所有元素相对该容器布局 */
#gameContainer {
    position: relative;
    width: 60vw; /* 让游戏区域占屏幕60% */
    height: 80vh; /* 让游戏区域占屏幕80% */
    margin: 0 auto;
    overflow: hidden;
    border: 2px solid rgba(255, 255, 255, 0.3);
}

/* 让球门固定在容器顶部 */
#goal {
    position: absolute;
    width: 30%; /* 自适应宽度 */
    left: 50%;
    top: 10%;
    transform: translateX(-50%);
}

/* 让球员和球可以移动 */
#player, #ball {
    position: absolute;
    width: 10%; /* 让球员和球大小自适应 */
    transition: left 0.1s ease-in-out, top 0.1s ease-in-out;
}

/* 提示信息 */
#gameMessage {
    font-size: 2vw;
    font-weight: bold;
    color: #ff4757;
    margin-top: 10px;
}
</style>
